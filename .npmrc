# Registry configuration
# @txi-dev packages are on GitHub Packages
@txi-dev:registry=https://npm.pkg.github.com/

# Authentication for GitHub Packages (for @txi-dev packages)
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Authentication for npm registry (for @trafficbyintent restricted packages)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}